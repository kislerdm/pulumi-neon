name: go-client
runtime: go
description: A minimal Go Pulumi program
config:
  pulumi:tags:
    value:
      pulumi:template: go

# for local tests only
plugins:
  providers:
    - name: neon
      path: ../bin
