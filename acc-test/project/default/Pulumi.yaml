name: acc-test
runtime: go
description: 'Acceptance tests: project, default'
config:
  pulumi:tags:
    value:
      pulumi:template: go
      test: true
      acctest: true
      resource: project
      type: default
      in_org: true

plugins:
  providers:
    - name: neon
      path:  ../../../bin
