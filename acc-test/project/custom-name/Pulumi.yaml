name: acc-test
runtime: go
description: 'Acceptance tests: project, custom name'
config:
  pulumi:tags:
    value:
      pulumi:template: go
      test: true
      acctest: true
      resource: project
      type: custom-name
      in_org: true

plugins:
  providers:
    - name: neon
      path:  ../../../bin
