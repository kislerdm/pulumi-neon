name: acc-test
runtime: go
description: Acceptance tests
config:
  pulumi:tags:
    value:
      pulumi:template: go
      test: true

plugins:
  providers:
    - name: neon
      path: ../../../../bin
